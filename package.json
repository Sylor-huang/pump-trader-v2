{
  "name": "pump-trader-v2",
  "version": "0.1.3",
  "description": "Unsigned Pump trade instruction builder.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "examples"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "prepack": "npm run build",
    "example:context": "npm run build && node --import tsx examples/get-trade-context.ts",
    "example:diagnose": "npm run build && node --import tsx examples/diagnose-market.ts",
    "example:market": "npm run build && node --import tsx examples/get-market-info.ts",
    "example:buy": "npm run build && node --import tsx examples/build-buy.ts",
    "example:sell": "npm run build && node --import tsx examples/build-sell.ts",
    "test": "node --test --import tsx"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@pump-fun/pump-sdk": "^1.36.0",
    "@pump-fun/pump-swap-sdk": "^1.16.0",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.4",
    "@types/node": "^25.9.1"
  }
}
